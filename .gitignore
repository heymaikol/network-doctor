# Go Binary
/network-doctor
/netdoc
/netdoc-sim

# Saved reports
/network-doctor-*.txt

# GoReleaser
dist/
# Vendored Go deps + the tarball of them COPR builds against, both produced by
# release-time hooks. Neither belongs in the source tarball.
/vendor/
/network-doctor-*-vendor.tar.gz

# Documentation site: the staging tree cmd/docsite writes and the site Jekyll
# renders from it. Both are built from docs/, the wiki, and site/, so neither is
# committed.
/_docsite/
/_site/

# Graphify
graphify-out/

# Node (nothing here is Go's business)
node_modules/
package.json
package-lock.json

# AI Stuff
.codex/
.claude/
CLAUDE.local.md
